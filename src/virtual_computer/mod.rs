pub fn create_virtual_computer() {
    println!("Creating virtual computer...");
    /// So a computer consists of a few main parts, which are made of smaller parts until we get to transistors
    /// While I would love to create a virtual computer completely from scratch (literally using logic gate functions like and(a, b)), that's not the aim of this project
    /// So for now I'll stick to a level of abstraction that is still relatively complex (to give me maximum control) but also quite simple to not waste much time
    /// 
    /// The few main components of a computer are:
    /// 
    /// Memory (RAM, ROM, Registers, Cache)
    /// CPU - Control Unit, ALU
    /// 
    pub fn run_code() {}
}